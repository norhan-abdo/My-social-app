import React from 'react'
import PostMessage from '../PostMessage/PostMessage'

export default function SavedPosts() {
    return (
        <PostMessage message={'No posts yet. Be the first one to publish.'} />
    )
}
